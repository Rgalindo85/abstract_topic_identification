stages:
  process_data:
    cmd: python src/prepare_data.py
    deps:
      - data/raw
      - config/main.yaml
      - src/prepare_data.py
    outs:
      - data/processed:
          persist: true
  train_model:
    cmd: python src/train_model.py
    deps:
      - data/processed
      - data/input_model
      - config/main.yaml
      - src/train_model.py
    # outs:
    #   - data/output_model:
    #      persist: true
