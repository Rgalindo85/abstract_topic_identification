stages:
  process_data:
    cmd: python src/prepare_data.py
    deps:
      - data/raw
      - config/main.yaml
      - src/prepare_data.py
    outs:
      - data/processed:
          persist: true
  train_model:
    cmd: python src/train.py
    deps:
      - data/processed
      # - data/input_model
      - config/main.yaml
      - src/train.py
    # outs:
    #   - data/output_model:
    #      persist: true
